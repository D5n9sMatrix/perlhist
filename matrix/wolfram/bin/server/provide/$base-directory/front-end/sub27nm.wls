#!/usr/bin/env wolframscript

(* Wolfram Script *)
(* Created by the Wolfram Language Plugin for IntelliJ, see http://wlplugin.halirutan.de/ *)
(* :Author: denis *)
(* :Date: 2021-11-07 *)

SetAttributes::argr[matrix + x]
RandomGraph::dist[matrix + x]
Set::write[matrix + x]
Range[27]

Input[$RecursionLimit::reclim2p p / pa "write number attribute limit: "]
Return exit(Enter)
ExactResult = p / p 1 year
